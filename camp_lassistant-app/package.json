{
  "name": "camp_lassistant-app",
  "version": "1.0.0",
  "scripts": {
    "dev:h5": "npm run serve",
    "build:h5": "npm run build",
    "serve": "uni-app serve",
    "build": "uni-app build"
  }
}
